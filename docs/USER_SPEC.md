# User Spec (US)

## Open questions & notes

- The US is the source of truth. NO AS can contradict the US. The US can't be changed by agents — only by the user.
- The US must be as simple as possible in the MyST mechanisms. Probably we should limit it to using `id` only: the user can use any MyST element, but clawloop reads only ids.
- Where does the user spec live?
